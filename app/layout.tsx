import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Providers } from "@/components/providers";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ResultShield Lite™ | Medical Claims Fraud Savings Calculator",
  description:
    "Instantly calculate the financial impact of fraud detection in medical insurance claims. Built for operations managers to demonstrate real savings potential.",
  openGraph: {
    title: "ResultShield Lite™ | Medical Claims Fraud Savings Calculator",
    description:
      "Instantly calculate the financial impact of fraud detection in medical insurance claims.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html
      lang="en"
      suppressHydrationWarning
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
